#include "monty.h"
/**
 * f_stack - prints the top
 * @head: stack head
 * @ccounter: line_number
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int ccounter)
{
	(void)head;
	(void)ccounter;
	bus.lifi = 0;
}
